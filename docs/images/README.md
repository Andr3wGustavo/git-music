# Preview Image Assets

Place your screenshot or mockup of the Git-Music Studio HUD interface in this directory:

- File name: `git-music-preview.png`
- Relative path: `docs/images/git-music-preview.png`

The root `README.md` is already configured to automatically render this image at the top of the documentation.
